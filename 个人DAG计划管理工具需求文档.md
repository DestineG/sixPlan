# 个人 DAG 计划管理工具需求文档

## 1. 项目概述

本项目是一个本地部署、面向个人计划管理场景的多用户 DAG 计划管理 Web 应用。

用户可以按照“工作、生活、学习”等领域创建多张独立计划图，通过节点和有向边表达计划阶段之间的先后关系或依赖关系，并持续维护每个节点的当前状态、时间信息、简短说明和 Markdown 附加信息。

应用通过本地命令启动并使用浏览器访问。默认只允许本机访问，也可以通过配置开放局域网访问。用户登录后只能访问自己的计划数据；正式数据由本地后端保存到 SQLite 数据库文件，并支持计划归档、导入、导出以及分权限的数据备份和恢复。

---

## 2. 产品目标

系统需要帮助用户完成以下事项：

1. 按领域组织不同类型的个人计划。
2. 为每个计划创建一张独立的 DAG 图。
3. 使用节点表示计划阶段、方向或目标。
4. 使用有向边表示节点之间的先后或依赖关系。
5. 持续更新节点当前状态和时间信息。
6. 为节点保存 Markdown 格式的附加信息。
7. 将单个计划导出为文件，并支持重新导入。
8. 按用户权限对个人数据或全站数据进行本地备份和恢复。
9. 所有正式数据保存在本机磁盘中。
10. 将暂时不再维护的计划归档，并在需要时恢复。
11. 支持多个用户注册和登录，并保证不同用户的数据彼此隔离。

---

## 3. 核心概念

系统的数据层级如下：

```text
用户 User
└── 领域 Area
    └── 计划 Plan
        ├── 节点 Node
        └── 有向边 Edge
```

### 3.1 用户

用户是数据隔离和权限控制的边界。每个用户拥有独立的领域、计划、节点、边和个人备份，不能查看或操作其他用户的计划数据。

用户角色包括：

- 普通用户
- 管理员

管理员负责账号管理、注册开关和全站备份恢复，但不能查看或编辑其他用户的计划内容。

### 3.2 领域

领域用于对计划进行分类。

领域示例：

- 工作
- 生活
- 学习
- 家庭
- 兴趣

用户可以创建、修改、删除和排序自己的领域。领域下只要存在普通或归档计划，就不允许删除；用户需要先移动或永久删除其中的全部计划。

### 3.3 计划

计划表示一张独立的 DAG 图。

计划示例：

- 实习准备
- 论文修改
- 软件开发
- 旅行安排
- 阅读计划

每个计划归属于一个领域，并具有自己的名称、说明、状态、节点和有向边。计划可以移动到当前用户的其他领域。

计划归档独立于计划状态。归档时记录归档时间，不改变计划原有的“规划中、进行中、已完成、暂停”状态；恢复后继续使用归档前的状态。

### 3.4 节点

节点表示计划中的一个阶段、方向或目标。

节点不是具体的每日待办，而是一个相对完整的计划单元。

节点示例：

- 学习推理基础
- 阅读 vLLM 源码
- 完成优化项目
- 准备简历
- 投递实习

### 3.5 有向边

有向边表示节点之间的先后关系或依赖关系。

例如：

```text
学习推理基础 → 阅读 vLLM 源码 → 完成优化项目 → 投递实习
```

系统需要保证计划图始终为 DAG，不允许自环、同方向重复边或任何会形成有向环的连接。

---

## 4. 用户基本流程

### 4.1 注册与登录

用户通过用户名和密码注册并登录。注册功能可以由管理员关闭；登录后才能访问计划数据。

### 4.2 创建领域

用户进入计划总览页后，可以创建一个领域，例如“工作”。

### 4.3 创建计划

用户在某个领域下创建计划，例如“实习准备”。

### 4.4 进入计划图

用户点击计划卡片后进入 DAG 编辑页。

### 4.5 创建节点

用户在画布中创建多个计划节点，并填写节点名称。

### 4.6 创建有向边

用户从一个节点的连接点拖动到另一个节点，创建有向边。

### 4.7 更新节点信息

用户点击节点，在右侧详情面板中编辑节点状态、时间、简短说明等信息。

### 4.8 编辑附加信息

用户点击“附加信息”按钮，打开 Markdown 编辑 Modal，在左侧编辑，在右侧实时查看渲染效果。

### 4.9 保存计划

节点基础信息和节点坐标采用防抖自动保存，边的创建和删除立即保存到本地 SQLite 数据库。Markdown 附加信息通过“保存”按钮或 `Ctrl + S` 显式保存。

### 4.10 归档与恢复计划

用户归档计划前需要确认。归档后的计划移入独立的“已归档”视图并变为只读，仍可打开查看、导出、恢复或永久删除。恢复操作无需二次确认，计划恢复到原领域并保留归档前状态。

普通计划不能直接永久删除。计划必须先归档，之后才能在“已归档”视图中经过二次确认永久删除。

### 4.11 导入与导出

用户可以导出单个计划，也可以一次选择一个或多个计划文件重新导入系统。领域支持包含全部普通及归档计划的整体导出和导入。

### 4.12 数据备份

普通用户可以备份和恢复自己的全部计划数据；管理员可以备份和恢复包含账号在内的全站数据。

---

## 5. 页面结构

系统主要包含以下页面：

1. 登录与注册页
2. 强制修改密码页
3. 计划总览页
4. DAG 编辑页
5. 设置与备份恢复页
6. 管理员用户管理页

---

## 6. 计划总览页需求

### 6.1 页面目标

计划总览页用于管理领域和计划，并提供进入计划 DAG 的入口。

### 6.2 页面布局

建议采用以下结构：

```text
┌────────────────────────────────────────────┐
│ 应用标题            导入  备份  设置  账号 │
├──────────────┬─────────────────────────────┤
│ 领域导航     │ 当前领域                    │
│              │                             │
│ 活跃计划     │ 计划卡片  计划卡片          │
│ 全部计划     │ 计划卡片  计划卡片          │
│ 工作         │                             │
│ 生活         │ 计划卡片  计划卡片          │
│ 学习         │                             │
│ 已归档       │                             │
│ 新建领域     │ 新建计划                    │
└──────────────┴─────────────────────────────┘
```

### 6.3 领域功能

需要支持：

- 创建领域
- 修改领域名称
- 删除领域
- 调整领域排序
- 切换当前领域
- 查看某个领域下的全部计划
- 领域内仍有普通或归档计划时拒绝删除，并明确提示原因

### 6.4 计划功能

需要支持：

- 创建计划
- 修改计划名称
- 修改计划说明
- 修改计划状态
- 归档计划
- 恢复归档计划
- 永久删除归档计划
- 将计划移动到其他领域
- 进入计划 DAG
- 导入计划
- 导出计划

### 6.5 活跃计划视图

“活跃计划”是用户登录后进入计划总览页时的默认视图，用于集中查看不同领域中当前正在进行的计划，避免逐个领域查找。

需要支持：

- 只显示未归档且计划自身状态为“进行中”的计划
- 不根据节点状态、日期或完成比例自动推断计划是否活跃
- 跨领域汇总，并在计划卡片中明确显示所属领域
- 计划状态改变或计划被归档后，立即从活跃计划视图移除
- 显示全部未归档活跃计划的数量，并显示各领域的活跃计划数量
- 没有活跃计划时显示明确的空状态
- 从活跃计划视图新建计划时，默认计划状态为“进行中”

### 6.6 计划卡片

计划卡片建议展示：

- 计划名称
- 所属领域
- 计划状态
- 节点数量
- 最近更新时间
- 简短说明

计划状态包括：

- 规划中
- 进行中
- 已完成
- 暂停

“已归档”不是计划状态。归档状态由独立的归档时间表示，因此归档和恢复不会丢失原计划状态。

### 6.7 已归档视图

需要支持：

- 普通领域和“全部计划”视图不显示归档计划
- 归档计划按原领域分组
- 每个领域分组内按归档时间从新到旧排列
- 显示计划名称、原领域、计划状态、节点数量和归档时间
- 打开归档计划的只读 DAG
- 导出归档计划
- 无需二次确认地恢复计划到原领域
- 经过二次确认后永久删除计划及其节点和边

---

## 7. DAG 编辑页需求

### 7.1 页面目标

DAG 编辑页用于创建和维护某一张计划图。

普通计划以编辑模式打开；归档计划以只读模式打开。只读模式需要明确显示“已归档”标识和归档时间，隐藏或禁用所有修改操作，但保留画布缩放、平移、适应画布、查看节点详情、查看 Markdown 和导出功能。

### 7.2 页面布局

建议采用以下结构：

```text
┌──────────────────────────────────────────────────────┐
│ 工作 / 实习准备       添加节点  自动布局  保存       │
├───────────────────────────────────┬──────────────────┤
│                                   │ 节点详情         │
│                                   │                  │
│          DAG 编辑画布             │ 名称             │
│                                   │ 状态             │
│                                   │ 时间             │
│                                   │ 简短说明         │
│                                   │ 附加信息         │
│                                   │                  │
├───────────────────────────────────┴──────────────────┤
│ 缩放  适应画布  自动布局  导入  导出                 │
└──────────────────────────────────────────────────────┘
```

### 7.3 画布功能

需要支持：

- 创建节点
- 删除节点
- 拖动节点
- 多选节点
- 画布缩放
- 画布平移
- 适应画布
- 自动布局
- 保存节点坐标
- 创建有向边
- 删除有向边
- 显示边的方向
- 检查有向环
- 拒绝会形成环的连接
- 重新加载后恢复节点位置和连接关系
- 归档计划打开时切换为只读画布

### 7.4 节点卡片

节点卡片建议展示：

- 节点名称
- 当前状态
- 开始时间
- 结束时间
- 简短说明摘要

节点状态包括：

- 未开始
- 进行中
- 已完成
- 暂停
- 放弃

不同状态需要在节点卡片中有清晰的视觉区分：未开始使用灰色、进行中使用蓝色、已完成使用绿色、暂停使用黄色、放弃使用红色。状态标签和节点卡片完整边框同时使用对应颜色，选中态使用独立外圈且不得覆盖状态色。界面始终保留状态文字，不能只依赖颜色传达状态。

### 7.5 节点基础操作

需要支持：

- 单击节点选中
- 双击节点快速编辑名称
- 删除节点并始终进行二次确认
- 拖动节点调整位置
- 从连接点创建边
- 查看节点的前驱节点
- 查看节点的后继节点

删除节点的确认提示需要说明相关边会被同步删除。节点和关联边必须在同一个数据库事务中删除，任一步骤失败时不得留下部分删除结果。

---

## 8. 节点详情面板需求

### 8.1 打开方式

用户单击节点后，在页面右侧打开节点详情面板。

### 8.2 编辑字段

详情面板需要支持编辑：

- 节点名称
- 当前状态
- 开始时间
- 结束时间
- 简短说明
- 附加信息

### 8.3 保存方式

开始时间和结束时间只保存本地日历日期，格式为 `YYYY-MM-DD`，不进行时区换算。开始时间和结束时间均可为空；两者同时存在时，结束日期不得早于开始日期。

日期输入框下方提供两行快捷操作。第一行“设置起止日期为今天”同时将开始日期和结束日期设为当前本地日期；第二行提供“+1天、+一周、+一个月、+三个月”，在当前结束日期上累加相应时长且不修改开始日期。结束日期为空时禁用第二行按钮。月份按自然月计算，目标月份没有对应日期时取该月最后一天；快捷修改沿用节点基础信息的防抖自动保存。

节点基础信息采用防抖自动保存，节点拖动结束后自动保存坐标。界面需要显示“保存中、已保存、保存失败”状态，保存失败时保留当前编辑内容并允许重试。

保存失败时需要给出明确提示。

所有更新携带实体版本号。后端发现版本已经变化时拒绝覆盖，前端提示数据已在其他页面更新，并要求用户刷新后重新编辑。

---

## 9. Markdown 附加信息 Modal

### 9.1 打开方式

用户在节点详情面板中点击“附加信息”按钮，打开 Modal。

### 9.2 Modal 布局

Modal 采用左右分栏：

```text
┌──────────────────────────────────────────────────────────┐
│ 节点名称 — 附加信息                              ×      │
├──────────────────────────┬───────────────────────────────┤
│ Markdown 编辑区          │ Markdown 预览区               │
│                          │                               │
│                          │                               │
│                          │                               │
├──────────────────────────┴───────────────────────────────┤
│ 保存状态                         取消          保存      │
└──────────────────────────────────────────────────────────┘
```

### 9.3 编辑区功能

需要支持：

- Markdown 文本编辑
- 标题
- 粗体
- 斜体
- 无序列表
- 有序列表
- 链接
- 引用
- 行内代码
- 代码块

### 9.4 预览区功能

需要支持 Markdown 实时渲染，包括：

- 标题
- 段落
- 列表
- 链接
- 引用
- 代码
- 代码块
- 表格

### 9.5 分栏交互

需要支持：

- 左右默认各占一半
- 拖动中间分隔线调整宽度
- 编辑区和预览区独立滚动
- Modal 全屏显示
- 关闭 Modal

### 9.6 保存交互

需要支持：

- 点击保存
- 点击取消
- `Ctrl + S` 保存
- 存在未保存修改时显示提示
- 关闭 Modal 时检查未保存内容
- 保存后更新节点附加信息

数据库中保存 Markdown 原文，预览内容由前端实时生成。

Markdown 不进行自动持久化。保存时携带节点版本号；发生并发冲突时不得覆盖服务器上的较新内容，且需要保留用户当前编辑文本，供用户刷新前自行处理。

---

## 10. DAG 校验需求

### 10.1 环检测

创建新边之前，系统必须检查新边是否会形成有向环。

例如已有：

```text
A → B → C
```

此时不允许创建：

```text
C → A
```

系统同时禁止：

- 节点连接到自身的自环
- 同一计划中起点和终点均相同的同方向重复边

### 10.2 引用完整性

系统需要保证：

- 每条边的起点节点存在
- 每条边的终点节点存在
- 起点和终点属于同一个计划
- 起点、终点和计划均属于当前登录用户
- 删除节点时在同一事务中同步删除相关边
- 归档计划中的节点和边不能修改

### 10.3 自动布局

系统提供自动布局功能，根据 DAG 结构重新排列节点。

自动布局完成后，需要保存新的节点坐标。

---

## 11. 本地数据存储

### 11.1 运行方式

第一版不打包为桌面程序，既支持通过本地命令启动，也支持通过 Docker Compose 部署。通过本地命令启动时，应用默认只监听本机地址：

```text
http://127.0.0.1:端口
```

监听地址可以通过配置改为 `0.0.0.0`，供受控局域网内的设备访问。Docker Compose 默认提供 `http://本地服务器IP:4173` 局域网入口。无论从本机还是局域网访问，用户都必须登录后才能访问业务数据。

需要公网访问时，支持在本地服务器同时运行 sixPlan、Caddy 和 frpc，在云服务器仅运行 frps。frpc 将云服务器公网 `80/443` 端口转发至本地 Caddy，由 Caddy 为云服务器公网 IPv4 自动申请和续期短期 IP 证书，并反向代理到 sixPlan。该模式不依赖域名，并同时保留局域网 HTTP 入口：

```text
局域网：http://本地服务器IP:4173
公网：https://云服务器公网IPv4
```

两个入口使用同一份数据库，但浏览器 Cookie 按主机和协议隔离，因此需要分别登录。公网 `80/443` 必须持续转发到本地 Caddy，以支持证书申请、续期和 HTTPS 访问。

### 11.2 架构

```text
浏览器前端
    ↓ HTTP API
本地后端服务
    ↓
SQLite 数据库文件
```

公网 IP HTTPS 部署架构为：

```text
公网浏览器
    ↓ HTTPS
云服务器 frps（仅负责 TCP 转发）
    ↓ FRP 加密连接
本地服务器 frpc
    ↓
本地 Caddy（证书与 HTTPS 终止）
    ↓ HTTP，固定容器网络
sixPlan Fastify + SQLite
```

后端技术栈确定为：

```text
Node.js
TypeScript
Fastify
Drizzle ORM
SQLite
```

### 11.3 存储内容

SQLite 数据库需要保存：

- 用户账号、角色和状态
- 登录会话
- 系统设置和注册开关
- 领域
- 计划
- 计划状态
- 计划归档时间
- 节点
- 节点状态
- 节点时间
- 节点简短说明
- Markdown 附加信息
- 节点画布坐标
- 有向边
- 创建时间
- 修改时间
- 可变实体版本号

### 11.4 数据目录

应用需要提供：

- 查看数据目录
- 打开数据目录
- 查看数据库文件位置
- 查看备份文件位置
- 查看导出文件位置

数据目录属于服务端主机。只有管理员从回环地址访问且部署配置明确允许时，才允许触发“打开数据目录”并打开服务端文件管理器。Docker 或公网部署必须禁用该能力。其他用户、局域网访问者和反向代理请求不得触发该操作；管理员可以查看服务端目录路径，普通用户不显示系统级目录入口。

### 11.5 用户认证与数据隔离

需要支持：

- 使用唯一用户名和密码注册
- 使用用户名和密码登录
- 退出登录
- 用户修改自己的密码
- 管理员关闭或重新开放注册，默认开放注册
- 通过启动命令创建管理员或将已有用户提升为管理员
- 管理员查看账号列表、禁用账号、重新启用账号和设置临时密码
- 被管理员重置密码的用户下次登录后强制修改密码
- 第一版不支持永久删除用户账号

密码只能保存安全哈希，不得保存明文或可逆密文。登录状态使用 `HttpOnly`、`SameSite=Strict` Cookie 保存，采用 7 天滑动过期策略。Cookie 的 `Secure` 属性按当前请求的实际协议决定：局域网直接 HTTP 请求不设置，经过可信 Caddy 的 HTTPS 请求设置。后端只信任配置的固定反向代理地址提供的转发协议头，不能允许客户端通过伪造头部影响安全判断。退出登录时清除与当前入口安全属性一致的 Cookie。退出登录、账号禁用、用户修改密码或管理员设置临时密码后，该用户已有会话需要立即失效。

所有领域、计划、节点、边、导入导出和用户级备份请求都必须按当前登录用户进行数据隔离。只在前端隐藏数据不算完成隔离，后端必须检查资源归属并拒绝跨用户访问。

管理员可以管理账号和全站备份，但不得通过管理员权限查看或编辑其他用户的领域、计划、节点、边或 Markdown 内容。

### 11.6 并发更新

所有可变实体使用版本号进行乐观并发控制。更新请求必须携带客户端读取到的版本号；版本不一致时，后端拒绝更新并返回明确的冲突结果，前端不得静默覆盖较新数据。

---

## 12. 导出需求

### 12.1 单个计划导出

用户可以将当前计划导出为一个独立文件。

建议文件名格式：

```text
计划名称.plan.json
```

### 12.2 导出内容

导出文件需要包含：

- 文件格式名称
- 文件格式版本
- 所属领域名称
- 计划基本信息
- 计划状态
- 归档状态和归档时间
- 全部节点
- 全部有向边
- 节点状态
- 节点时间
- 节点简短说明
- Markdown 附加信息
- 节点画布坐标

### 12.3 导出操作

需要支持：

- 从计划总览页导出
- 从 DAG 编辑页导出
- 选择保存位置
- 导出成功提示
- 导出失败提示

单计划导出文件保持明文 JSON，不提供加密。导出前需要提示文件可能包含节点说明和 Markdown 等敏感内容。用户只能导出自己的计划；归档计划允许导出。

### 12.4 领域导出

用户可以从领域操作菜单导出整个领域。领域文件使用版本化的 `.area.json` 明文 JSON 格式，包含领域名称、领域时间信息，以及领域内全部普通和归档计划的完整计划信息、节点、边、Markdown、坐标和原始时间。导出顺序不影响重新导入结果。

---

## 13. 导入需求

### 13.1 导入入口

用户可以从计划总览页选择一个或多个计划文件进行导入。一次选择多个文件时视为一次批量导入流程。

### 13.2 文件校验

导入前需要检查：

- 文件是否为支持的格式
- 文件格式版本是否兼容
- 计划基本信息是否完整
- 节点数据是否完整
- 边引用的节点是否存在
- 是否存在自环或同方向重复边
- 导入后的图是否为 DAG
- 日期字段是否为合法的 `YYYY-MM-DD`，且结束日期不早于开始日期

### 13.3 ID 处理

导入为新计划时，需要重新生成：

- 计划 ID
- 节点 ID
- 边 ID

并重新建立节点和边之间的映射关系。

导入永远创建新计划，不允许通过导入覆盖已有计划。同一领域允许计划重名；出现重名时需要提示用户，但用户确认后仍可继续导入。

### 13.4 领域处理

计划导出文件需要携带所属领域名称。导入时按以下规则处理：

- 领域字段非空且当前用户存在同名领域时，询问是否使用该领域，也允许改选其他已有领域
- 领域字段非空但同名领域不存在时，询问创建该领域或改选已有领域
- 领域字段为空时，要求用户选择一个已有领域
- 批量导入时可以将本次选择记录为默认 action，后续文件不再重复询问
- 默认 action 只在本次批量导入流程中生效，流程完成或取消后立即清除，不写入数据库或浏览器持久存储

### 13.5 批量导入结果

批量导入按文件独立校验和写入，不要求整批原子成功。合法文件继续导入，失败文件不产生任何部分数据，流程结束后按文件汇总成功结果和失败原因。

### 13.6 导入结果

导入成功后：

- 将计划添加到指定领域
- 保存到 SQLite 数据库
- 在计划总览页显示新计划
- 支持立即进入导入后的 DAG

导入文件中的归档状态需要保留。归档计划导入成功后进入“已归档”视图并保持只读；普通计划进入目标领域的普通计划列表。

### 13.7 领域导入

计划总览页的“导入”菜单分别提供“导入计划”和“导入领域”入口。领域导入文件必须先完整校验，全部计划有效后才允许写入；领域创建或合并以及全部计划、节点和边的写入必须在同一个数据库事务中完成，任一写入失败时全部回滚。

导入领域与已有领域同名时，由用户选择“合并到已有领域”或“创建新领域”。选择创建时默认名称为“原领域名（导入）”并允许修改；领域名称仍须满足当前用户内唯一。导入到已有领域时，文件中的计划均作为独立副本加入，不覆盖任何已有计划。无论创建或合并，都重新生成计划、节点和边 ID，同时保留计划状态、归档状态、Markdown、坐标和原始时间。

### 13.8 v2 计划快照格式

单计划导入导出统一使用 `sixplan-plan-snapshot`、`version: 2`，不兼容 v1。快照中的节点使用计划内唯一且不可变的稳定 `key` 互相引用，数据库 UUID 不写入文件。`key` 以小写 ASCII 字母开头，只允许小写字母、数字和连字符，最长 64 字符。手工创建节点时由服务端生成 key，导入节点保留合法 key。

快照使用 `markdown` 保存节点 Markdown，坐标使用 `position: { x, y }`。技术字段允许省略：计划状态默认 `planning`，节点状态默认 `not_started`，日期默认 `null`，文本默认空字符串，归档状态默认未归档，时间戳由导入器补齐。只要任一节点缺少坐标，就对整个新计划执行从左到右的自动布局。边只使用 `{ source, target }`，语义为 target 依赖 source。

### 13.9 v2 增量格式

现有计划扩展使用 `sixplan-plan-changeset`、`version: 2`。增量文件由用户明确选择目标计划，可修改计划名称、说明和状态，可新增、更新、删除节点以及新增、删除连接；不得归档、恢复、移动领域或删除计划。增量操作通过稳定 node key 引用节点，新增节点不得与当前计划或同一增量中的 key 冲突。

计划维护 `graphRevision`，节点或连接的结构变化使其递增。增量文件携带 `baseRevision`。应用前必须在最新图上重新模拟全部操作并检查 key、日期、引用、重复边和环；如果提示词生成后图版本已变化但变更仍合法，前端刷新预览并要求再次确认；如果出现缺失 key、冲突或成环则拒绝。应用在单个事务中完成，新增无坐标节点只在保留现有坐标的前提下进行增量布局。删除节点或连接使用红色预览并额外确认。

### 13.10 AI 提示词工作台

系统不内置大模型、不调用模型 API，也不保存模型 API Key。总览页提供“AI 生成新计划”和“AI 扩展现有计划”入口，流程固定为：用户输入自然语言想法，sixPlan 构造只读提示词，用户复制或下载 TXT 到任意外部模型，再粘贴或上传模型 JSON，系统校验、预览、确认后事务写入。用户不需要自己编写 JSON 提示词。

高级选项默认折叠。扩展计划的操作范围默认为“所有节点”，也可切换为“叶节点”；叶节点严格指没有后继节点的节点，与节点状态无关。选择所有节点时提供完整图上下文，选择叶节点时提供叶节点及其直接前驱作为只读上下文。界面必须显示两种范围各自的节点数量，大上下文只提示风险，不得静默缩减。

提示词必须区分操作目标和只读参考节点，固定提供完整的 v2 增量协议，并逐项声明界面字段与 JSON 字段的映射。针对当前任务的指导不能裁剪协议能力，也不能因为用户提到某个字段就禁止其同时要求的计划、节点或连接操作。“附加信息”“详细内容”“详细计划”和 Markdown 对应 `changes.markdown`，不得写入 `summary`；Markdown 更新使用完整覆盖语义，原 Markdown 默认不进入上下文。

服务端只严格校验 Schema、只读常量、用户明确选择的操作范围、节点 key 和引用、日期、重复边、有向环、版本等可客观判定的规则，不得通过关键词推断用户的自然语言意图并据此限制字段或结构操作。“所有节点”或“叶节点”表示允许操作的现有节点范围，本身不代表必须修改范围内每一个节点；模型是否完整实现语义要求由提示词指导，并通过变更预览交由用户确认。

提示词和原始 JSON 不永久保留。返回文本中恰好包含一个 Markdown JSON 围栏时，提取前必须询问用户。校验失败时可基于错误信息和原 JSON 生成只读修复提示词。`targetPlanName` 和 `baseRevision` 必须作为只读常量逐字复制，模型不得翻译、简称、改写或猜测。

### 13.11 严格校验与临时会话

所有 JSON Schema 均为严格模式，未知字段、拼写错误和 v1 文件一律拒绝，不做静默修复。除结构校验外，还需校验真实存在的日历日期、时间顺序、唯一 key、边引用、自环、重复边和有向环。快照目标领域始终由用户确认，文件中的 `areaName` 只能预选已有领域或建议创建名称。

粘贴和上传使用同一验证器。上传内容流式写入用户隔离的临时目录，节点和边逐项解析；确认前形成临时导入会话，默认 24 小时后自动清理。最终写入前再次解析和验证文件，不信任前端预览缓存。临时会话和文件必须按用户隔离，其他用户不能读取或应用。

### 13.12 导入规模配置

用户可在设置页配置个人节点数、连接数、单节点 Markdown 和单文件限制，`0` 表示不额外设置个人限制，不能绕过服务端硬上限。服务端默认硬上限为：单文件 512 MB、节点 50,000、连接 250,000、单节点 Markdown 5 MB、每用户临时空间 2 GB、每用户并发 2、全站并发 8、任务超时 30 分钟、会话 24 小时；所有硬上限均可通过环境变量配置。

大图画布需要启用视口内渲染，避免全图动画。预览只展示有代表性的部分节点和边，同时显示完整统计。布局计算不得阻塞大文件的上传解析。

---

## 14. 数据备份与恢复

### 14.1 备份

普通用户可以备份自己的全部业务数据，包括：

- 所有领域
- 所有计划
- 所有节点
- 所有有向边
- 所有 Markdown 附加信息
- 所有节点坐标

用户级备份只包含当前用户的领域、普通和归档计划、节点、边及相关内容，不包含账号密码、登录会话、其他用户数据或系统设置。

管理员可以创建全站备份。全站备份包含全部账号、角色、账号状态、系统设置和全部用户的业务数据，但不得包含可复用的明文密码或登录会话。

备份文件采用带格式名称和版本信息的封装格式，并支持可选密码加密。用户设置密码后，恢复时必须提供正确密码；系统不得保存备份密码。

### 14.2 恢复

普通用户可以从用户级备份恢复自己的全部业务数据。恢复只覆盖当前用户的领域和计划数据，不修改账号、密码、角色、会话、系统设置或其他用户的数据。

管理员可以从全站备份恢复整个系统。全站恢复将完整替换当前账号、设置和业务数据，不执行合并。

恢复前需要：

- 检查备份文件格式
- 检查备份版本
- 提示当前数据将发生变化
- 创建恢复前备份
- 验证加密备份密码

恢复必须在事务或等效的原子替换流程中执行。恢复失败时保留恢复前的当前数据，并显示明确失败原因。恢复成功后，需要重新加载相关领域和计划。

全站恢复成功后，所有现有登录会话立即失效，所有用户必须重新登录。用户级恢复不影响当前用户或其他用户的账号和会话。

---

## 15. 数据模型

### 15.1 User

```text
User
- id
- username
- password_hash
- role
- is_disabled
- must_change_password
- version
- created_at
- updated_at
```

`username` 全局唯一。`role` 至少包含 `user` 和 `admin`。账号禁用后不得创建新会话，已有会话立即失效。

### 15.2 Session

```text
Session
- id
- user_id
- token_hash
- expires_at
- last_active_at
- version
- created_at
```

数据库只保存会话令牌哈希。会话最多保持 7 天，并在有效访问时滑动更新过期时间。

### 15.3 SystemSetting

```text
SystemSetting
- key
- value
- version
- updated_at
```

系统设置至少包含注册是否开放。只有管理员可以修改系统设置。

### 15.4 Area

```text
Area
- id
- user_id
- name
- sort_order
- version
- created_at
- updated_at
```

同一用户的领域名称必须唯一，以保证导入时能够按领域名称进行确定性匹配。

### 15.5 Plan

```text
Plan
- id
- area_id
- name
- description
- status
- archived_at
- version
- created_at
- updated_at
```

`archived_at` 为空表示普通计划，非空表示归档计划。计划状态不包含“已归档”。

永久删除归档计划时，需要在一个事务中删除计划、节点和边；失败时不得保留部分删除结果。

### 15.6 Node

```text
Node
- id
- plan_id
- title
- status
- start_date
- end_date
- summary
- extra_content
- position_x
- position_y
- version
- created_at
- updated_at
```

`start_date` 和 `end_date` 使用 `YYYY-MM-DD` 日期格式，不保存时区信息。

### 15.7 Edge

```text
Edge
- id
- plan_id
- source_node_id
- target_node_id
- version
- created_at
- updated_at
```

同一计划内需要对 `source_node_id + target_node_id` 建立唯一约束。数据库外键和后端业务校验共同保证边的节点、计划和用户归属一致。

### 15.8 权限矩阵

| 能力 | 普通用户 | 管理员 |
|---|---|---|
| 管理自己的领域、计划和 DAG | 支持 | 支持 |
| 查看其他用户的计划内容 | 不支持 | 不支持 |
| 备份和恢复自己的业务数据 | 支持 | 支持 |
| 查看、禁用、启用账号 | 不支持 | 支持 |
| 设置用户临时密码 | 不支持 | 支持 |
| 开放或关闭注册 | 不支持 | 支持 |
| 创建和恢复全站备份 | 不支持 | 支持 |
| 查看服务端数据路径 | 不支持 | 支持 |
| 打开服务端数据目录 | 不支持 | 仅从服务主机回环地址访问且部署配置允许时支持 |

---

## 16. 技术栈

前端采用：

```text
React
TypeScript
Vite
React Flow
```

推荐配套：

```text
Zustand
React Router
react-markdown
CodeMirror
Dagre
fetch 或 Axios
```

主要职责：

```text
React          页面和组件开发
TypeScript     类型约束和数据模型
Vite           开发服务器和项目构建
React Flow     DAG 画布、节点、边、拖动、缩放和连接
Zustand        前端状态管理
React Router   页面路由
react-markdown Markdown 渲染
CodeMirror     Markdown 编辑
Dagre          DAG 自动布局
```

后端采用：

```text
Node.js        本地服务运行时
TypeScript     类型约束和业务模型
Fastify        HTTP API 和认证中间件
Drizzle ORM    SQLite 数据访问和迁移
SQLite         正式数据持久化
```

---

## 17. 前端页面与组件

### 17.1 页面

```text
LoginPage
RegisterPage
ForcePasswordChangePage
PlanOverviewPage
PlanGraphPage
SettingsPage
AdminUsersPage
```

### 17.2 核心组件

```text
AuthForm
AreaSidebar
PlanCard
PlanEditorModal
ArchivedPlanList
ArchivePlanDialog
DeleteArchivedPlanDialog
GraphToolbar
PlanGraphCanvas
PlanNode
NodeDetailPanel
MarkdownEditorModal
ImportPlanModal
ExportPlanModal
BackupRestorePanel
AccountMenu
AdminUserTable
```

### 17.3 推荐目录

```text
frontend/
├── src/
│   ├── components/
│   │   ├── area/
│   │   ├── auth/
│   │   ├── admin/
│   │   ├── plan/
│   │   ├── graph/
│   │   ├── node/
│   │   ├── markdown/
│   │   ├── import-export/
│   │   └── layout/
│   ├── pages/
│   │   ├── LoginPage.tsx
│   │   ├── RegisterPage.tsx
│   │   ├── ForcePasswordChangePage.tsx
│   │   ├── PlanOverviewPage.tsx
│   │   ├── PlanGraphPage.tsx
│   │   ├── SettingsPage.tsx
│   │   └── AdminUsersPage.tsx
│   ├── stores/
│   ├── services/
│   ├── types/
│   ├── utils/
│   ├── App.tsx
│   └── main.tsx
├── package.json
└── vite.config.ts
```

---

## 18. API 功能范围

本地后端需要提供以下接口能力：

### 18.1 认证与账号接口

- 查询注册是否开放
- 注册普通用户
- 登录和退出登录
- 查询当前用户
- 修改当前用户密码
- 完成临时密码强制修改
- 验证会话并执行 7 天滑动续期

### 18.2 管理员接口

- 查询用户列表，不返回密码哈希或用户计划内容
- 禁用和启用用户
- 为用户设置临时密码
- 查询和修改注册开关
- 执行全站备份和恢复
- 查询服务端数据、备份和导出目录
- 仅在本机请求中打开服务端数据目录

### 18.3 领域接口

- 查询领域列表
- 创建领域
- 修改领域
- 删除领域
- 更新领域排序
- 删除前检查领域内是否仍有普通或归档计划

### 18.4 计划接口

- 查询计划列表
- 按计划状态筛选未归档计划，用于跨领域活跃计划视图
- 查询计划详情
- 创建计划
- 修改计划
- 归档计划
- 查询按领域分组的归档计划
- 恢复归档计划
- 永久删除归档计划
- 将计划移动到其他领域
- 修改计划状态

普通计划不提供永久删除能力。归档、恢复、移动和永久删除均需要验证计划属于当前用户；归档计划的其他写操作需要被后端拒绝。

### 18.5 节点接口

- 查询计划节点
- 创建节点
- 修改节点
- 删除节点
- 批量更新节点坐标

删除节点和关联边必须在同一个事务中完成。节点创建、修改、删除及坐标更新均需要拒绝归档计划或跨用户资源。

### 18.6 有向边接口

- 查询计划有向边
- 创建有向边
- 删除有向边
- 创建前检查自环、同方向重复边和有向环

### 18.7 导入导出接口

- 导出单个计划
- 批量导入计划并返回逐文件结果
- 导出完整领域
- 以创建或合并模式事务导入完整领域
- 导出和恢复当前用户的全部业务数据
- 创建和恢复可选密码加密的备份

### 18.8 API 通用规则

- 除注册状态、注册和登录外，所有接口都需要有效会话
- 所有业务资源查询和写入都在后端按当前用户限制范围
- 所有可变实体的更新请求携带版本号
- 版本冲突返回明确且可区分的冲突错误，不执行覆盖
- 校验失败、权限不足、资源不存在和保存失败需要返回可供前端展示的明确错误
- 密码哈希、会话令牌哈希及其他敏感认证字段不得返回前端
- 仅信任明确配置的固定反向代理地址提供的协议转发头

---

## 19. 验收标准

### 19.1 多领域与多计划

- 可以创建工作、生活等多个领域。
- 每个领域下可以创建多张计划。
- 点击计划后可以进入对应 DAG。
- 不同计划的数据彼此独立。
- 可以将普通计划移动到当前用户的其他领域。
- 领域内存在普通或归档计划时不能删除领域。
- 登录后默认进入“活跃计划”视图，跨领域显示所有未归档且计划状态为“进行中”的计划。
- 活跃计划只按计划自身状态判断，不根据节点状态或日期推断。
- 计划退出“进行中”状态或被归档后，不再出现在活跃计划视图中。

### 19.2 计划归档

- 归档前显示确认弹窗，并说明计划会进入只读状态。
- 归档计划从普通领域和“全部计划”视图中隐藏。
- “已归档”视图按原领域分组，组内按归档时间倒序。
- 归档计划可以打开查看和导出，但不能修改计划、节点、边、坐标或 Markdown。
- 恢复归档计划无需二次确认，并回到原领域及归档前计划状态。
- 普通计划不能永久删除；归档计划只有经过二次确认才能永久删除。
- 刷新和重启应用后，归档时间、原状态、领域归属和排序保持一致。

### 19.3 DAG 编辑

- 可以创建、移动和删除节点。
- 可以创建和删除有向边。
- 节点和边在刷新页面后仍然存在。
- 创建会形成环的边时，系统拒绝该操作。
- 创建自环或同方向重复边时，系统拒绝该操作。
- 自动布局能够重新排列节点。
- 删除节点前始终进行确认，删除后相关边同步消失，不产生部分删除。

### 19.4 节点信息

- 可以编辑节点名称和状态。
- 可以设置开始时间和结束时间。
- 可以填写简短说明。
- 节点卡片能够展示主要信息。
- 五种节点状态的标签和完整卡片边框使用各自状态色，选中节点后仍保留原状态色。
- 结束日期早于开始日期时不能保存。
- 基础字段和坐标自动保存，并显示保存状态和错误。
- 多页面并发修改时，旧版本更新被拒绝且不会静默覆盖新数据。

### 19.5 Markdown 附加信息

- 可以打开附加信息 Modal。
- 可以在左侧编辑 Markdown。
- 右侧能够实时渲染。
- 可以保存和取消。
- 重新打开后内容保持一致。
- Markdown 只在点击保存或按 `Ctrl + S` 时持久化。
- Markdown 发生版本冲突时保留当前编辑文本，不覆盖服务器内容。

### 19.6 登录、权限与本地存储

- 数据保存在 SQLite 文件中。
- 关闭并重新启动应用后数据仍然存在。
- 浏览器缓存被清理后正式数据仍然存在。
- 注册开放时可以创建唯一用户名账号，管理员关闭注册后不能创建新账号。
- 不同用户无法通过页面或直接调用 API 查看或修改彼此数据。
- 管理员可以管理账号，但不能查看其他用户计划内容。
- 禁用账号、修改密码或退出登录后，相关旧会话立即失效。
- 临时密码用户登录后必须先修改密码才能进入计划页面。
- 默认只能从本机访问，配置局域网监听后仍必须登录。
- 只有管理员从服务主机回环地址访问且部署配置允许时可以打开数据目录。
- 局域网 HTTP 登录 Cookie 不设置 `Secure`，经可信 Caddy 的公网 HTTPS 登录 Cookie 设置 `Secure`。
- 非可信来源伪造协议转发头不能改变 Cookie 安全属性或获得代理信任。

### 19.7 导入导出

- 可以导出一张完整计划。
- 导出的文件可以重新导入。
- 导入后节点、边、状态、Markdown 和坐标保持一致。
- 非法文件能够被识别并提示。
- 归档计划导出再导入后仍为归档状态，并保留原计划状态和归档时间。
- 导入始终生成新 ID，不覆盖同名计划。
- 批量导入允许部分文件成功，并逐文件显示成功结果或失败原因。
- 领域匹配、创建和改选行为符合导入规则，本批默认 action 不会延续到下次导入。
- 可以导出包含普通及归档计划的完整领域，并选择合并到同名领域或创建新领域。
- 领域导入重新生成全部实体 ID且不覆盖已有计划；任一计划无效或写入失败时不产生部分领域数据。

### 19.8 备份与恢复

- 普通用户只能备份和恢复自己的业务数据。
- 管理员可以创建和恢复全站备份，但不能借此在页面中浏览其他用户计划。
- 未加密和密码加密备份均可正确恢复，错误密码需要明确提示。
- 恢复前自动创建备份；恢复失败时当前数据保持不变。
- 用户级恢复不影响账号、会话、系统设置或其他用户。
- 全站恢复采用完整覆盖，完成后所有会话失效。

### 19.9 Docker Compose 与公网访问

- 可以只启动 sixPlan 容器，通过 `http://本地服务器IP:4173` 在局域网访问。
- 可以同时启动 sixPlan、Caddy 和 frpc，在保留局域网 HTTP 的同时通过 `https://云服务器公网IPv4` 访问。
- 云服务器只需部署 frps，不需要部署 sixPlan、Caddy、证书工具或业务数据。
- 公网入口使用受信任的公网 IPv4 短期证书，并由本地 Caddy 自动申请和续期，不依赖域名。
- 公网 HTTPS 与局域网 HTTP 使用同一份 SQLite 数据，两个入口的登录 Cookie 相互独立。
- Docker 和公网部署不能调用“打开服务端数据目录”。
- 容器重启或升级后数据库和证书状态保持；未明确要求时不得删除数据卷。

---

## 20. 开发阶段建议

### 第一阶段：项目基础与本地后端

完成：

- React、TypeScript 和 Vite 项目初始化
- Node.js、TypeScript 和 Fastify 后端初始化
- Drizzle ORM 和 SQLite 数据库迁移
- 本地命令启动前后端
- Docker Compose 构建和启动
- 默认本机监听和可配置局域网监听
- 可选的本地 Caddy、frpc 与云端仅 frps 公网 IP HTTPS 部署

### 第二阶段：认证与数据隔离

完成：

- 注册、登录、退出和会话管理
- 密码修改和临时密码强制修改
- 管理员初始化命令、账号管理和注册开关
- 用户数据归属与后端访问控制

### 第三阶段：DAG 基础能力

完成：

- React Flow 画布
- 自定义节点
- 节点增删改
- 有向边增删
- 节点拖动
- 环检测
- 自动布局
- 乐观并发控制

### 第四阶段：节点信息维护

完成：

- 节点详情面板
- 节点状态
- 节点时间
- 简短说明
- 节点卡片状态展示
- 防抖自动保存和保存状态
- 日期格式与先后关系校验

### 第五阶段：Markdown 附加信息

完成：

- Markdown Modal
- 左侧编辑
- 右侧实时预览
- 分栏调整
- 保存与未保存提示
- Markdown 保存冲突处理

### 第六阶段：多领域、多计划与归档

完成：

- 领域管理
- 计划管理
- 计划总览页
- 计划状态
- 计划切换
- 默认活跃计划视图和跨领域状态筛选
- 计划跨领域移动
- 归档视图、只读查看、恢复和永久删除

### 第七阶段：持久化完善

完成：

- 领域、计划、节点和边持久化
- 节点坐标批量保存
- 删除事务和数据库完整性约束
- 数据目录和管理员本机访问限制

### 第八阶段：导入导出与备份

完成：

- 单计划导出
- 单计划和批量计划导入
- 导入校验
- 用户级备份和恢复
- 管理员全站备份和恢复
- 可选密码加密和恢复失败回滚
